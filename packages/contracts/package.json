{
  "name": "@personal-crm/contracts",
  "private": true,
  "version": "0.0.0",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "node -e \"console.log('TODO: add contracts build when shared schemas exist')\"",
    "lint": "pnpm exec ultracite check .",
    "test": "node -e \"console.log('TODO: add tests for @personal-crm/contracts')\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
